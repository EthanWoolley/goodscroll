# Test dependencies for the backend suite. Runtime dependencies live in
# backend/requirements.txt; the repo-root requirements-dev.txt pulls both this
# file and the linter in, so `make install` covers everything.
-r requirements.txt
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
# Required by starlette's TestClient, which the route tests drive the ASGI app
# through in-process (no sockets, no running server).
httpx>=0.27.0
